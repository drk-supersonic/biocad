diff --git a/node_modules/gantt-task-react/dist/index.css b/node_modules/gantt-task-react/dist/index.css
index ff26a91..face962 100644
--- a/node_modules/gantt-task-react/dist/index.css
+++ b/node_modules/gantt-task-react/dist/index.css
@@ -154,12 +154,14 @@
 }
 
 ._1rLuZ {
-  stroke: #e6e4e4;
+  stroke: #c9c9c9;
+  stroke-width: 1.2;
 }
 
 ._2q1Kt {
   text-anchor: middle;
-  fill: #555;
+  fill: #2b2b2b;
+  font-weight: 600;
   -webkit-touch-callout: none;
   -webkit-user-select: none;
   -moz-user-select: none;
diff --git a/node_modules/gantt-task-react/dist/index.js b/node_modules/gantt-task-react/dist/index.js
index 626bb39..bdce49b 100644
--- a/node_modules/gantt-task-react/dist/index.js
+++ b/node_modules/gantt-task-react/dist/index.js
@@ -701,6 +701,7 @@ var TopPartOfCalendar = function TopPartOfCalendar(_ref) {
     key: value + "text",
     y: yText,
     x: xText,
+    dominantBaseline: "central",
     className: styles$5.calendarTopText
   }, value));
 };
@@ -747,7 +748,7 @@ var Calendar = function Calendar(_ref) {
           y1Line: 0,
           y2Line: headerHeight,
           xText: xText,
-          yText: topDefaultHeight * 0.9
+          yText: topDefaultHeight / 2
         }));
       }
     }
@@ -762,7 +763,10 @@ var Calendar = function Calendar(_ref) {
 
     for (var i = 0; i < dateSetup.dates.length; i++) {
       var date = dateSetup.dates[i];
-      var bottomValue = getLocaleMonth(date, locale);
+      var bottomValue = getCachedDateTimeFormat(locale, {
+        month: "short"
+      }).format(date);
+      bottomValue = bottomValue.replace(bottomValue[0], bottomValue[0].toLocaleUpperCase());
       bottomValues.push(React__default.createElement("text", {
         key: bottomValue + date.getFullYear(),
         y: headerHeight * 0.8,
@@ -785,9 +789,9 @@ var Calendar = function Calendar(_ref) {
           value: topValue,
           x1Line: columnWidth * i,
           y1Line: 0,
-          y2Line: topDefaultHeight,
+          y2Line: headerHeight,
           xText: xText,
-          yText: topDefaultHeight * 0.9
+          yText: topDefaultHeight / 2
         }));
       }
     }
@@ -810,13 +814,24 @@ var Calendar = function Calendar(_ref) {
         topValue = getLocaleMonth(date, locale) + ", " + date.getFullYear();
       }
 
-      var bottomValue = "W" + getWeekNumberISO8601(date);
+      var weekNumber = getWeekNumberISO8601(date);
+      var bottomValueFull = "Неделя " + weekNumber;
+      var bottomValue;
+
+      if (columnWidth >= 70) {
+        bottomValue = "Нед. " + weekNumber;
+      } else if (columnWidth >= 40) {
+        bottomValue = "Нед..";
+      } else {
+        bottomValue = "Н..";
+      }
+
       bottomValues.push(React__default.createElement("text", {
         key: date.getTime(),
         y: headerHeight * 0.8,
-        x: columnWidth * (i + +rtl),
+        x: columnWidth * i + columnWidth * 0.5,
         className: styles$5.calendarBottomText
-      }, bottomValue));
+      }, bottomValue, React__default.createElement("title", null, bottomValueFull)));
 
       if (topValue) {
         if (i !== dates.length - 1) {
@@ -825,9 +840,9 @@ var Calendar = function Calendar(_ref) {
             value: topValue,
             x1Line: columnWidth * i + weeksCount * columnWidth,
             y1Line: 0,
-            y2Line: topDefaultHeight,
+            y2Line: headerHeight,
             xText: columnWidth * i + columnWidth * weeksCount * 0.5,
-            yText: topDefaultHeight * 0.9
+            yText: topDefaultHeight / 2
           }));
         }
 
@@ -863,9 +878,9 @@ var Calendar = function Calendar(_ref) {
           value: topValue,
           x1Line: columnWidth * (i + 1),
           y1Line: 0,
-          y2Line: topDefaultHeight,
+          y2Line: headerHeight,
           xText: columnWidth * (i + 1) - getDaysInMonth(date.getMonth(), date.getFullYear()) * columnWidth * 0.5,
-          yText: topDefaultHeight * 0.9
+          yText: topDefaultHeight / 2
         }));
       }
     }
@@ -902,7 +917,7 @@ var Calendar = function Calendar(_ref) {
           y1Line: 0,
           y2Line: topDefaultHeight,
           xText: columnWidth * i + ticks * columnWidth * 0.5,
-          yText: topDefaultHeight * 0.9
+          yText: topDefaultHeight / 2
         }));
       }
     }
@@ -940,7 +955,7 @@ var Calendar = function Calendar(_ref) {
           y1Line: 0,
           y2Line: topDefaultHeight,
           xText: columnWidth * (i + topPosition),
-          yText: topDefaultHeight * 0.9
+          yText: topDefaultHeight / 2
         }));
       }
     }
@@ -995,6 +1010,28 @@ var Calendar = function Calendar(_ref) {
       bottomValues = _getCalendarValuesFor6[1];
   }
 
+  var dayDividers = dateSetup.dates.map(function (d, i) {
+    return React__default.createElement("line", {
+      key: "day-divider-" + i,
+      x1: columnWidth * i,
+      y1: headerHeight * 0.5,
+      x2: columnWidth * i,
+      y2: headerHeight,
+      style: {
+        stroke: "var(--border)"
+      }
+    });
+  });
+  var monthDayDivider = React__default.createElement("line", {
+    key: "month-day-divider",
+    x1: 0,
+    y1: headerHeight * 0.5,
+    x2: columnWidth * dateSetup.dates.length,
+    y2: headerHeight * 0.5,
+    style: {
+      stroke: "var(--border)"
+    }
+  });
   return React__default.createElement("g", {
     className: "calendar",
     fontSize: fontSize,
@@ -1005,7 +1042,7 @@ var Calendar = function Calendar(_ref) {
     width: columnWidth * dateSetup.dates.length,
     height: headerHeight,
     className: styles$5.calendarHeader
-  }), bottomValues, " ", topValues);
+  }), dayDividers, monthDayDivider, bottomValues, " ", topValues);
 };
 
 // A type of promise-like that resolves synchronously and supports only one observer
@@ -1774,6 +1811,12 @@ var TaskItem = function TaskItem(props) {
       isTextInside = _useState2[0],
       setIsTextInside = _useState2[1];
 
+  var _useState3 = React.useState(task.name),
+      label = _useState3[0],
+      setLabel = _useState3[1];
+
+  var measuredForRef = React.useRef(null);
+
   React.useEffect(function () {
     switch (task.typeInternal) {
       case "milestone":
@@ -1793,11 +1836,43 @@ var TaskItem = function TaskItem(props) {
         break;
     }
   }, [task, isSelected]);
-  React.useEffect(function () {
+  React.useLayoutEffect(function () {
     if (textRef.current) {
-      setIsTextInside(textRef.current.getBBox().width < task.x2 - task.x1);
+      // Компонент переиспользуется между обновлениями (тот же task.id),
+      // а bbox всегда меряет то, что СЕЙЧАС отрисовано в DOM — это может
+      // быть усечённая строка с предыдущего рендера (другая ширина бара,
+      // например после смены Дня/Недели/Месяца). measuredForRef хранит,
+      // для какой именно пары (имя, ширина бара) мы уже честно замеряли —
+      // в отличие от сравнения label !== task.name, это не путает "усекли
+      // только что" с "остался старый label от прошлой геометрии", и не
+      // зацикливается: обычное сравнение label !== task.name всегда истинно
+      // сразу после усечения (в этом и есть смысл усечения), и если положить
+      // label в зависимости эффекта, это даёт бесконечный ререндер.
+      var barWidth = task.x2 - task.x1;
+      var measureKey = task.name + "|" + barWidth;
+
+      if (measuredForRef.current !== measureKey) {
+        measuredForRef.current = measureKey;
+        if (label !== task.name) {
+          setLabel(task.name);
+          return;
+        }
+      }
+
+      var innerWidth = barWidth - 16; // отступ по 8px с каждой стороны
+      var bbox = textRef.current.getBBox();
+
+      if (bbox.width <= innerWidth || task.name.length <= 1) {
+        setIsTextInside(true);
+      } else {
+        var avgCharWidth = bbox.width / task.name.length;
+        var maxChars = Math.max(1, Math.floor(innerWidth / avgCharWidth) - 1);
+        var truncated = task.name.length > maxChars ? task.name.slice(0, maxChars) + "\u2026" : task.name;
+        setIsTextInside(true);
+        setLabel(truncated);
+      }
     }
-  }, [textRef, task]);
+  }, [textRef, task, label]);
 
   var getX = function getX() {
     var width = task.x2 - task.x1;
@@ -1844,9 +1919,9 @@ var TaskItem = function TaskItem(props) {
   }, taskItem, React__default.createElement("text", {
     x: getX(),
     y: task.y + taskHeight * 0.5,
-    className: isTextInside ? style.barLabel :  style.barLabelOutside,
+    className: style.barLabel,
     ref: textRef
-  }, task.name));
+  }, label, React__default.createElement("title", null, task.name)));
 };
 
 var TaskGanttContent = function TaskGanttContent(_ref) {
diff --git a/node_modules/gantt-task-react/dist/index.modern.js b/node_modules/gantt-task-react/dist/index.modern.js
index b372269..2ca2adf 100644
--- a/node_modules/gantt-task-react/dist/index.modern.js
+++ b/node_modules/gantt-task-react/dist/index.modern.js
@@ -700,6 +700,7 @@ var TopPartOfCalendar = function TopPartOfCalendar(_ref) {
     key: value + "text",
     y: yText,
     x: xText,
+    dominantBaseline: "central",
     className: styles$5.calendarTopText
   }, value));
 };
@@ -746,7 +747,7 @@ var Calendar = function Calendar(_ref) {
           y1Line: 0,
           y2Line: headerHeight,
           xText: xText,
-          yText: topDefaultHeight * 0.9
+          yText: topDefaultHeight / 2
         }));
       }
     }
@@ -761,7 +762,10 @@ var Calendar = function Calendar(_ref) {
 
     for (var i = 0; i < dateSetup.dates.length; i++) {
       var date = dateSetup.dates[i];
-      var bottomValue = getLocaleMonth(date, locale);
+      var bottomValue = getCachedDateTimeFormat(locale, {
+        month: "short"
+      }).format(date);
+      bottomValue = bottomValue.replace(bottomValue[0], bottomValue[0].toLocaleUpperCase());
       bottomValues.push(React.createElement("text", {
         key: bottomValue + date.getFullYear(),
         y: headerHeight * 0.8,
@@ -784,9 +788,9 @@ var Calendar = function Calendar(_ref) {
           value: topValue,
           x1Line: columnWidth * i,
           y1Line: 0,
-          y2Line: topDefaultHeight,
+          y2Line: headerHeight,
           xText: xText,
-          yText: topDefaultHeight * 0.9
+          yText: topDefaultHeight / 2
         }));
       }
     }
@@ -809,13 +813,24 @@ var Calendar = function Calendar(_ref) {
         topValue = getLocaleMonth(date, locale) + ", " + date.getFullYear();
       }
 
-      var bottomValue = "W" + getWeekNumberISO8601(date);
+      var weekNumber = getWeekNumberISO8601(date);
+      var bottomValueFull = "Неделя " + weekNumber;
+      var bottomValue;
+
+      if (columnWidth >= 70) {
+        bottomValue = "Нед. " + weekNumber;
+      } else if (columnWidth >= 40) {
+        bottomValue = "Нед..";
+      } else {
+        bottomValue = "Н..";
+      }
+
       bottomValues.push(React.createElement("text", {
         key: date.getTime(),
         y: headerHeight * 0.8,
-        x: columnWidth * (i + +rtl),
+        x: columnWidth * i + columnWidth * 0.5,
         className: styles$5.calendarBottomText
-      }, bottomValue));
+      }, bottomValue, React.createElement("title", null, bottomValueFull)));
 
       if (topValue) {
         if (i !== dates.length - 1) {
@@ -824,9 +839,9 @@ var Calendar = function Calendar(_ref) {
             value: topValue,
             x1Line: columnWidth * i + weeksCount * columnWidth,
             y1Line: 0,
-            y2Line: topDefaultHeight,
+            y2Line: headerHeight,
             xText: columnWidth * i + columnWidth * weeksCount * 0.5,
-            yText: topDefaultHeight * 0.9
+            yText: topDefaultHeight / 2
           }));
         }
 
@@ -862,9 +877,9 @@ var Calendar = function Calendar(_ref) {
           value: topValue,
           x1Line: columnWidth * (i + 1),
           y1Line: 0,
-          y2Line: topDefaultHeight,
+          y2Line: headerHeight,
           xText: columnWidth * (i + 1) - getDaysInMonth(date.getMonth(), date.getFullYear()) * columnWidth * 0.5,
-          yText: topDefaultHeight * 0.9
+          yText: topDefaultHeight / 2
         }));
       }
     }
@@ -901,7 +916,7 @@ var Calendar = function Calendar(_ref) {
           y1Line: 0,
           y2Line: topDefaultHeight,
           xText: columnWidth * i + ticks * columnWidth * 0.5,
-          yText: topDefaultHeight * 0.9
+          yText: topDefaultHeight / 2
         }));
       }
     }
@@ -939,7 +954,7 @@ var Calendar = function Calendar(_ref) {
           y1Line: 0,
           y2Line: topDefaultHeight,
           xText: columnWidth * (i + topPosition),
-          yText: topDefaultHeight * 0.9
+          yText: topDefaultHeight / 2
         }));
       }
     }
@@ -994,6 +1009,28 @@ var Calendar = function Calendar(_ref) {
       bottomValues = _getCalendarValuesFor6[1];
   }
 
+  var dayDividers = dateSetup.dates.map(function (d, i) {
+    return React.createElement("line", {
+      key: "day-divider-" + i,
+      x1: columnWidth * i,
+      y1: headerHeight * 0.5,
+      x2: columnWidth * i,
+      y2: headerHeight,
+      style: {
+        stroke: "var(--border)"
+      }
+    });
+  });
+  var monthDayDivider = React.createElement("line", {
+    key: "month-day-divider",
+    x1: 0,
+    y1: headerHeight * 0.5,
+    x2: columnWidth * dateSetup.dates.length,
+    y2: headerHeight * 0.5,
+    style: {
+      stroke: "var(--border)"
+    }
+  });
   return React.createElement("g", {
     className: "calendar",
     fontSize: fontSize,
@@ -1004,7 +1041,7 @@ var Calendar = function Calendar(_ref) {
     width: columnWidth * dateSetup.dates.length,
     height: headerHeight,
     className: styles$5.calendarHeader
-  }), bottomValues, " ", topValues);
+  }), dayDividers, monthDayDivider, bottomValues, " ", topValues);
 };
 
 // A type of promise-like that resolves synchronously and supports only one observer
@@ -1773,6 +1810,12 @@ var TaskItem = function TaskItem(props) {
       isTextInside = _useState2[0],
       setIsTextInside = _useState2[1];
 
+  var _useState3 = useState(task.name),
+      label = _useState3[0],
+      setLabel = _useState3[1];
+
+  var measuredForRef = useRef(null);
+
   useEffect(function () {
     switch (task.typeInternal) {
       case "milestone":
@@ -1792,11 +1835,43 @@ var TaskItem = function TaskItem(props) {
         break;
     }
   }, [task, isSelected]);
-  useEffect(function () {
+  React.useLayoutEffect(function () {
     if (textRef.current) {
-      setIsTextInside(textRef.current.getBBox().width < task.x2 - task.x1);
+      // Компонент переиспользуется между обновлениями (тот же task.id),
+      // а bbox всегда меряет то, что СЕЙЧАС отрисовано в DOM — это может
+      // быть усечённая строка с предыдущего рендера (другая ширина бара,
+      // например после смены Дня/Недели/Месяца). measuredForRef хранит,
+      // для какой именно пары (имя, ширина бара) мы уже честно замеряли —
+      // в отличие от сравнения label !== task.name, это не путает "усекли
+      // только что" с "остался старый label от прошлой геометрии", и не
+      // зацикливается: обычное сравнение label !== task.name всегда истинно
+      // сразу после усечения (в этом и есть смысл усечения), и если положить
+      // label в зависимости эффекта, это даёт бесконечный ререндер.
+      var barWidth = task.x2 - task.x1;
+      var measureKey = task.name + "|" + barWidth;
+
+      if (measuredForRef.current !== measureKey) {
+        measuredForRef.current = measureKey;
+        if (label !== task.name) {
+          setLabel(task.name);
+          return;
+        }
+      }
+
+      var innerWidth = barWidth - 16; // отступ по 8px с каждой стороны
+      var bbox = textRef.current.getBBox();
+
+      if (bbox.width <= innerWidth || task.name.length <= 1) {
+        setIsTextInside(true);
+      } else {
+        var avgCharWidth = bbox.width / task.name.length;
+        var maxChars = Math.max(1, Math.floor(innerWidth / avgCharWidth) - 1);
+        var truncated = task.name.length > maxChars ? task.name.slice(0, maxChars) + "\u2026" : task.name;
+        setIsTextInside(true);
+        setLabel(truncated);
+      }
     }
-  }, [textRef, task]);
+  }, [textRef, task, label]);
 
   var getX = function getX() {
     var width = task.x2 - task.x1;
@@ -1843,9 +1918,9 @@ var TaskItem = function TaskItem(props) {
   }, taskItem, React.createElement("text", {
     x: getX(),
     y: task.y + taskHeight * 0.5,
-    className: isTextInside ? style.barLabel :  style.barLabelOutside,
+    className: style.barLabel,
     ref: textRef
-  }, task.name));
+  }, label, React.createElement("title", null, task.name)));
 };
 
 var TaskGanttContent = function TaskGanttContent(_ref) {
